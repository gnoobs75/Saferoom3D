---
allowed-tools: Bash(git status:*), Bash(git diff:*), Bash(git log:*)
description: Show git status and recent commits
---

Show the current state of the repository:

1. Run `git status` to show changed files
2. Run `git log --oneline -5` to show recent commits
3. Summarize what's uncommitted
